## Coding Guidelines

### Red/Green TDD

- 必须采用 Red/Green TDD：先写测试并运行，确认它因正确的原因失败，再写最小实现让它变绿。
- **优先增强或改造已有用例，而非一律新增**。已有用例已经覆盖同一条路径时，把新断言并进去；只有当新行为确属另一条路径、并进去会让单个用例同时钉住互不相关的性质时才另开。

### KISS & YAGNI

- 单次使用的代码不做抽象，不为不可能的场景写错误处理；200 行能写成 50 行就重写。

### 其他

- 生成代码前先解释方案，等用户确认
- Worktree 必须创建在项目根目录下 `.worktrees/`，创建后复制 `.claude/settings.local.json` 到新 worktree。
- AI 生成的临时文档（计划、设计等）不提交到仓库
